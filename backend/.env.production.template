# ═══════════════════════════════════════════════
# FarmerConnect Backend — Production Environment Template
# Copy this to .env on your production server
# ═══════════════════════════════════════════════

PORT=5002
NODE_ENV=production
LOG_LEVEL=warn

# Use DATABASE_URL for cloud databases (Render, Railway, Heroku, etc.)
DATABASE_URL=postgresql://user:password@host:5432/farmer_connect_prod?sslmode=require

# OR use individual variables:
# DB_HOST=your-production-db-host
# DB_PORT=5432
# DB_NAME=farmer_connect_prod
# DB_USER=your_prod_user
# DB_PASSWORD=your_strong_prod_password
# DB_POOL_MAX=30

# JWT — MUST change this in production!
JWT_SECRET=CHANGE_THIS_TO_A_64_CHAR_RANDOM_STRING
JWT_EXPIRE=7d

# Frontend URL for CORS
FRONTEND_URL=https://your-production-domain.com
