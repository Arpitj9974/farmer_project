# ═══════════════════════════════════════════════════════
# FarmerConnect Backend — Environment Variables
# ═══════════════════════════════════════════════════════
# Copy this file as .env and fill in your actual values
# NEVER commit the .env file to Git!
# ═══════════════════════════════════════════════════════

# ─── Server ───
PORT=5002
NODE_ENV=development
LOG_LEVEL=info

# ─── Database (PostgreSQL) ───
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=farmer_connect
DB_USER=postgres
DB_PASSWORD=your_secure_database_password
DB_POOL_MAX=20

# ─── JWT Authentication ───
# Generate a strong random string: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_secure_jwt_secret_min_64_chars
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# ─── Frontend URL (CORS — CRITICAL for production!) ───
# In production, set this to your Vercel domain
FRONTEND_URL=http://localhost:3006

# ─── Gemini AI (Google) ───
# Get your key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# ─── Data.gov.in API (APMC Market Prices) ───
# Get your key at: https://data.gov.in/user/register
# Without this key, market prices will use mock data
DATA_GOV_KEY=your_data_gov_api_key_here
